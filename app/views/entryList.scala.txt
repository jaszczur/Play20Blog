@(entries: Seq[models.BlogEntry])[
  @for(entry <- entries) {
    {
      "id": "@entry.id",
      "title": "@entry.title",
      "creationDate": "@entry.creationDate",
      "content": "@entry.content"
    },
  }
]

